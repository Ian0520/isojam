[project]
name = "isojam-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "python-multipart",
]

[dependency-groups]
dev = [
    "pytest",
    "httpx2",
]